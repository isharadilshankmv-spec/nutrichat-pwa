// Run this once to generate icons — or replace icon-192.png and icon-512.png
// with your own images. For now the app will work without custom icons.
// 
// To generate: open browser console on any page and run:
//   const c=document.createElement('canvas');c.width=192;c.height=192;
//   const ctx=c.getContext('2d');ctx.fillStyle='#0d0d0d';ctx.fillRect(0,0,192,192);
//   ctx.fillStyle='#c8f06e';ctx.font='bold 110px sans-serif';ctx.textAlign='center';
//   ctx.textBaseline='middle';ctx.fillText('N',96,96);
//   const a=document.createElement('a');a.download='icon-192.png';a.href=c.toDataURL();a.click();
